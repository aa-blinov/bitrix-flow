name: CI

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  secrets:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: gitleaks/gitleaks-action@v2

  quality:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm run format:check
      - run: npm run lint
      - run: npm test
      - run: npm run build

  ui:
    # Скриншот-тесты: отдельная база с фиксированными данными и мок Битрикса,
    # иначе сравнивать картинки не с чем — боевые задачи меняются каждую минуту.
    runs-on: ubuntu-latest
    timeout-minutes: 25
    # Тот же образ, в котором снимаются эталоны: иначе рендеринг шрифтов
    # отличается и попиксельное сравнение падает на ровном месте.
    container:
      image: mcr.microsoft.com/playwright:v1.63.0-noble
    services:
      mongodb:
        image: mongo:7
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: node scripts/seed-test-db.mjs
        env:
          MONGO_URL: mongodb://mongodb:27017
      - run: npx playwright test
        env:
          CI: 'true'
          TEST_MONGO_URL: mongodb://mongodb:27017
      - uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 7
